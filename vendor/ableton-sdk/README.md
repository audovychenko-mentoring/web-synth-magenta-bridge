# Ableton SDK Vendor Folder

Put Ableton Extensions SDK beta tarballs here after downloading them from Ableton Centercode.

Expected examples:

```text
ableton-create-extension-*.tgz
ableton-extensions-sdk-*.tgz
```

The tarballs are intentionally ignored by git.
